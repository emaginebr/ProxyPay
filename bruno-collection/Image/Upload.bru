meta {
  name: Upload
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/image/upload/:productId?sortOrder=0
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  productId: 1
}

params:query {
  sortOrder: 0
}

body:multipart-form {
  file: @file()
}
