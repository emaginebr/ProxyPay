meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/category/:storeSlug/delete/:categoryId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  storeSlug: minha-loja
  categoryId: 1
}
