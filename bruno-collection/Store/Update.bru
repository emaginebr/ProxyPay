meta {
  name: Update
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/store/update
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "storeId": 1,
    "name": "Loja Atualizada"
  }
}
