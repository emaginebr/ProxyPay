meta {
  name: Delete
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/storeUser/:storeSlug/delete/:storeUserId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  storeSlug: minha-loja
  storeUserId: 1
}
