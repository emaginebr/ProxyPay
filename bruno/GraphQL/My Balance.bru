meta {
  name: My Balance
  type: graphql
  seq: 5
}

post {
  url: {{baseUrl}}/graphql
  body: graphql
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:graphql {
  {
    myBalance {
      balance
      totalCredits
      totalDebits
      transactionCount
    }
  }
}
